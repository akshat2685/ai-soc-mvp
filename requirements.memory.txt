# =============================================================================
# AI SOC Memory Platform — Python dependencies
# Kept SEPARATE from the existing SOC deps so this subsystem is self-contained.
# Install:  pip install -r requirements.memory.txt
# =============================================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- Layer 1: PostgreSQL ---
psycopg[binary]==3.2.3

# --- Layer 2: Qdrant ---
qdrant-client==1.12.1

# --- Layer 3: Neo4j ---
neo4j==5.27.0

# --- Semantic embeddings (local, offline-capable) ---
sentence-transformers==3.3.1
numpy==2.2.1

# --- HTTP client for SOC integration ---
httpx==0.28.1

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
